# Local development environment — NOT committed (gitignored).
# These are placeholder values that pass the Zod schema's format validation.
# Replace with real values from your service providers before running the app.
DATABASE_URL=postgresql://storyintovideo:storyintovideo_dev_password@localhost:5432/storyintovideo_dev
DATABASE_URL_UNPOOLED=postgresql://storyintovideo:storyintovideo_dev_password@localhost:5432/storyintovideo_dev
AUTH_SECRET=d46a5c998rqe1eykFHfJt0UBMLdeSLhC89bmwrF0KTOKVjqTbvEY=
AUTH_URL=http://localhost:3000

# ── Google OAuth (optional — both required to enable) ──
# Create at: https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

OPENAI_API_KEY=sk-test-key
REPLICATE_API_TOKEN=r8_test_token
ELEVENLABS_API_KEY=test-elevenlabs-key

# ── Replicate Model IDs (optional — override the Zod defaults) ──
# REPLICATE_SDXL_MODEL=stability-ai/sdxl:39ed52f2a788939d832ec6675557c771a6b0f9b6ce8bcd3ff0f4e4f3f1e0a6e3
# ⚠️ REQUIRED FOR CHARACTER CONSISTENCY: Set this to a real lucataco/sdxl-ipadapter
# version hash before relying on IP-Adapter.
# REPLICATE_SDXL_IPADAPTER_MODEL=lucataco/sdxl-ipadapter:<real-version-hash>

# ── Image Moderation Policy (optional, default: true) ──
# true  = fail-open (flagged=false, moderationSkipped=true for unknown shapes)
# false = fail-closed (flagged=true with 'unknown-output-shape' for unknown shapes)
# IMAGE_MODERATION_FAIL_OPEN=false

STRIPE_SECRET_KEY=sk_test_stripe
STRIPE_WEBHOOK_SECRET=whsec_test
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test
R2_ACCOUNT_ID=test-account-id
R2_ACCESS_KEY_ID=test-access-key
R2_SECRET_ACCESS_KEY=test-secret-key
R2_BUCKET_UPLOADS=siv-uploads-test
R2_BUCKET_GENERATED=siv-generated-test
R2_BUCKET_VIDEOS=siv-videos-test
INNGEST_EVENT_KEY=test-event-key
INNGEST_SIGNING_KEY=test-signing-key
RESEND_API_KEY=re_test
UPSTASH_REDIS_REST_URL=https://example.upstash.io
UPSTASH_REDIS_REST_TOKEN=test-token
SENTRY_DSN=https://example@sentry.io/1
FFMPEG_PATH=/usr/bin/ffmpeg
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
