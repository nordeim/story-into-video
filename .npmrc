# pnpm configuration
# Hoist workspace dependencies (helps shadcn/ui CLI find peer deps)
node-linker=hoisted

# Strict peer dependency checks (warn but don't fail)
strict-peer-dependencies=false

# Auto-install peers
auto-install-peers=true

# Use the local pnpm for shadcn CLI calls
shamefully-hoist=true
